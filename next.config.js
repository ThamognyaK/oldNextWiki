/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { unoptimized: true },
    output: "export",
  basePath: "/austin-utexas",

}

module.exports = nextConfig
